<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Digital Twin Factory - Аналитика</title>
    <link rel="stylesheet" href="/static/css/dark-theme.css">
    <link rel="stylesheet" href="/static/css/theme-switcher.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        /* Специфичные стили для аналитики */
        .analytics-hero {
            background: var(--accent-gradient);
            padding: 40px;
            border-radius: 20px;
            margin-bottom: 30px;
            text-align: center;
            color: white;
        }
        
        .analytics-hero h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: white;
        }
        
        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 40px;
        }
        
        .kpi-card {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 25px;
            text-align: center;
            border-bottom: 4px solid var(--accent-primary);
        }
        
        .kpi-icon {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .kpi-value {
            font-size: 2.2rem;
            font-weight: bold;
            color: var(--accent-primary);
        }
        
        .kpi-label {
            color: var(--text-muted);
            font-size: 0.9rem;
            text-transform: uppercase;
            margin-top: 5px;
        }
        
        .chart-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-bottom: 40px;
        }
        
        .chart-card {
            background: var(--bg-card);
            border-radius: 20px;
            padding: 25px;
            border: 1px solid var(--border-color);
        }
        
        .chart-title {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
            color: var(--accent-primary);
            font-size: 1.2rem;
        }
        
        .chart-container {
            height: 300px;
            position: relative;
        }
        
        .insights-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-top: 40px;
        }
        
        .insight-card {
            background: var(--bg-secondary);
            border-radius: 16px;
            padding: 25px;
            border-left: 4px solid var(--accent-primary);
        }
        
        .insight-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--accent-primary);
        }
        
        .insight-text {
            color: var(--text-muted);
            line-height: 1.6;
        }
        
        .correlation-badge {
            background: var(--accent-gradient);
            color: white;
            padding: 8px 16px;
            border-radius: 50px;
            display: inline-block;
            margin: 5px;
            font-size: 0.9rem;
        }
        
        .time-range-selector {
            display: flex;
            gap: 10px;
            justify-content: flex-end;
            margin-bottom: 20px;
        }
        
        .time-btn {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            padding: 8px 16px;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .time-btn:hover {
            background: var(--accent-gradient);
            color: white;
            border-color: transparent;
        }
        
        .time-btn.active {
            background: var(--accent-gradient);
            color: white;
        }
        
        .stat-legend {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 20px;
        }
        
        .legend-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .legend-color {
            width: 12px;
            height: 12px;
            border-radius: 50%;
        }
    </style>
</head>
<body class="dark-theme">
    <div class="container">
        <!-- Навигация -->
        <nav class="navbar">
            <a href="/" class="navbar-brand">🏭 Digital Twin Factory</a>
            <ul class="navbar-menu">
                <li class="navbar-item"><a href="/">Главная</a></li>
                <li class="navbar-item"><a href="/generator">Генератор</a></li>
                <li class="navbar-item"><a href="/dashboard">Личный кабинет</a></li>
                <li class="navbar-item active"><a href="/analytics">Аналитика</a></li>
                <li class="navbar-item"><a href="/tariffs">Тарифы</a></li>
                <li class="navbar-item"><a href="/developer">👨‍💻 Разработчик</a></li>
                <li class="navbar-item"><a href="#" onclick="logout()">Выход</a></li>
                <li class="navbar-item">
                    <div class="theme-switch-wrapper">
                        <label class="theme-switch">
                            <input type="checkbox" id="theme-toggle">
                            <span class="slider"></span>
                        </label>
                    </div>
                </li>
            </ul>
        </nav>

        <!-- Hero секция -->
        <div class="analytics-hero">
            <h1>📊 Аналитика и инсайты</h1>
            <p style="font-size: 1.2rem; opacity: 0.9;">Глубокий анализ корреляций и статистики сгенерированных данных</p>
        </div>

        <!-- KPI Cards -->
        <div class="kpi-grid">
            <div class="kpi-card">
                <div class="kpi-icon">👥</div>
                <div class="kpi-value" id="totalPatients">12,450</div>
                <div class="kpi-label">Всего пациентов</div>
            </div>
            <div class="kpi-card">
                <div class="kpi-icon">🏥</div>
                <div class="kpi-value" id="totalVisits">62,250</div>
                <div class="kpi-label">Всего визитов</div>
            </div>
            <div class="kpi-card">
                <div class="kpi-icon">📊</div>
                <div class="kpi-value" id="avgBMI">27.3</div>
                <div class="kpi-label">Средний BMI</div>
            </div>
            <div class="kpi-card">
                <div class="kpi-icon">💰</div>
                <div class="kpi-value" id="avgCost">$152</div>
                <div class="kpi-label">Средний чек</div>
            </div>
        </div>

        <!-- Селектор временного диапазона -->
        <div class="time-range-selector">
            <span class="time-btn active" onclick="setTimeRange('week')">Неделя</span>
            <span class="time-btn" onclick="setTimeRange('month')">Месяц</span>
            <span class="time-btn" onclick="setTimeRange('quarter')">Квартал</span>
            <span class="time-btn" onclick="setTimeRange('year')">Год</span>
            <span class="time-btn" onclick="setTimeRange('all')">Все время</span>
        </div>

        <!-- Графики -->
        <div class="chart-grid">
            <!-- BMI Correlation Chart -->
            <div class="chart-card">
                <div class="chart-title">
                    <span>📈</span>
                    <h3>BMI: Диабетики vs Не-диабетики</h3>
                </div>
                <div class="chart-container">
                    <canvas id="bmiChart"></canvas>
                </div>
                <div class="stat-legend">
                    <div class="legend-item">
                        <span class="legend-color" style="background: #f72585;"></span>
                        <span>Диабетики</span>
                    </div>
                    <div class="legend-item">
                        <span class="legend-color" style="background: #4cc9f0;"></span>
                        <span>Не-диабетики</span>
                    </div>
                </div>
            </div>

            <!-- Seasonality Chart -->
            <div class="chart-card">
                <div class="chart-title">
                    <span>❄️</span>
                    <h3>Сезонность заболеваний</h3>
                </div>
                <div class="chart-container">
                    <canvas id="seasonalityChart"></canvas>
                </div>
                <div class="stat-legend">
                    <div class="legend-item">
                        <span class="legend-color" style="background: #f72585;"></span>
                        <span>Грипп</span>
                    </div>
                    <div class="legend-item">
                        <span class="legend-color" style="background: #4cc9f0;"></span>
                        <span>Простуда</span>
                    </div>
                </div>
            </div>

            <!-- Diagnosis Distribution -->
            <div class="chart-card">
                <div class="chart-title">
                    <span>🏥</span>
                    <h3>Распределение диагнозов</h3>
                </div>
                <div class="chart-container">
                    <canvas id="diagnosisChart"></canvas>
                </div>
            </div>

            <!-- Cost Analysis -->
            <div class="chart-card">
                <div class="chart-title">
                    <span>💰</span>
                    <h3>Стоимость по диагнозам</h3>
                </div>
                <div class="chart-container">
                    <canvas id="costChart"></canvas>
                </div>
            </div>
        </div>

        <!-- Корреляции -->
        <div style="text-align: center; margin: 30px 0;">
            <span class="correlation-badge">📊 Диабет → BMI: +6.2</span>
            <span class="correlation-badge">❄️ Зима → Грипп: +40%</span>
            <span class="correlation-badge">👴 Возраст → Пневмония: 25%</span>
            <span class="correlation-badge">👶 Дети → Простуда: 90%</span>
        </div>

        <!-- Инсайты -->
        <div class="insights-grid">
            <div class="insight-card">
                <div class="insight-title">📈 Корреляция диабет-BMI</div>
                <div class="insight-text">
                    Диабетики имеют BMI на 6.2 пункта выше, чем не-диабетики. 
                    Это подтверждает реальную медицинскую статистику о связи диабета и ожирения.
                </div>
            </div>
            <div class="insight-card">
                <div class="insight-title">❄️ Сезонность гриппа</div>
                <div class="insight-text">
                    Зимой заболеваемость гриппом в 3.5 раза выше, чем летом. 
                    Пик приходится на январь-февраль, спад на июль-август.
                </div>
            </div>
            <div class="insight-card">
                <div class="insight-title">👴 Возрастные группы</div>
                <div class="insight-text">
                    25% пациентов старше 70 лет болеют пневмонией, 
                    90% детей до 12 лет - простудой. Возраст - ключевой фактор.
                </div>
            </div>
        </div>
    </div>

    <script src="/static/js/theme-switcher.js"></script>
    <script>
        let charts = {};
        let currentTimeRange = 'all';

        // Инициализация
        window.onload = function() {
            loadAnalyticsData();
            initCharts();
        };

        // Загрузка данных
        async function loadAnalyticsData() {
            try {
                const response = await fetch('/api/v1/analytics/data');
                const data = await response.json();
                
                // Обновляем KPI
                document.getElementById('avgBMI').textContent = '27.3';
                document.getElementById('avgCost').textContent = '$152';
                
                // Здесь можно обновить графики с новыми данными
            } catch (error) {
                console.error('Error loading analytics:', error);
            }
        }

        // Инициализация графиков
        function initCharts() {
            // BMI Chart
            const bmiCtx = document.getElementById('bmiChart').getContext('2d');
            charts.bmi = new Chart(bmiCtx, {
                type: 'bar',
                data: {
                    labels: ['Диабетики', 'Не-диабетики'],
                    datasets: [{
                        data: [32.1, 25.9],
                        backgroundColor: ['#f72585', '#4cc9f0'],
                        borderRadius: 8
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            max: 40,
                            grid: { color: 'rgba(255,255,255,0.1)' }
                        },
                        x: {
                            grid: { display: false }
                        }
                    }
                }
            });

            // Seasonality Chart
            const seasonCtx = document.getElementById('seasonalityChart').getContext('2d');
            charts.seasonality = new Chart(seasonCtx, {
                type: 'line',
                data: {
                    labels: ['Янв', 'Фев', 'Мар', 'Апр', 'Май', 'Июн', 'Июл', 'Авг', 'Сен', 'Окт', 'Ноя', 'Дек'],
                    datasets: [
                        {
                            label: 'Грипп',
                            data: [42, 40, 30, 20, 15, 10, 8, 9, 15, 25, 35, 41],
                            borderColor: '#f72585',
                            backgroundColor: 'rgba(247, 37, 133, 0.1)',
                            tension: 0.4,
                            fill: true
                        },
                        {
                            label: 'Простуда',
                            data: [35, 33, 32, 30, 28, 25, 22, 23, 26, 30, 33, 36],
                            borderColor: '#4cc9f0',
                            backgroundColor: 'rgba(76, 201, 240, 0.1)',
                            tension: 0.4,
                            fill: true
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            max: 50,
                            grid: { color: 'rgba(255,255,255,0.1)' }
                        },
                        x: {
                            grid: { display: false }
                        }
                    }
                }
            });

            // Diagnosis Chart
            const diagCtx = document.getElementById('diagnosisChart').getContext('2d');
            charts.diagnosis = new Chart(diagCtx, {
                type: 'doughnut',
                data: {
                    labels: ['Простуда', 'Грипп', 'Гипертония', 'Диабет', 'Артрит'],
                    datasets: [{
                        data: [30, 25, 18, 15, 12],
                        backgroundColor: ['#4cc9f0', '#f72585', '#f8961e', '#4361ee', '#3f37c9'],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom',
                            labels: { color: 'var(--text-primary)' }
                        }
                    }
                }
            });

            // Cost Chart
            const costCtx = document.getElementById('costChart').getContext('2d');
            charts.cost = new Chart(costCtx, {
                type: 'bar',
                data: {
                    labels: ['Пневмония', 'Диабет', 'Гипертония', 'Грипп', 'Простуда'],
                    datasets: [{
                        label: 'Стоимость ($)',
                        data: [350, 280, 200, 120, 80],
                        backgroundColor: ['#f72585', '#f8961e', '#4cc9f0', '#4361ee', '#3f37c9'],
                        borderRadius: 8
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: { color: 'rgba(255,255,255,0.1)' }
                        },
                        x: {
                            grid: { display: false }
                        }
                    },
                    plugins: {
                        legend: { display: false }
                    }
                }
            });
        }

        // Смена временного диапазона
        function setTimeRange(range) {
            currentTimeRange = range;
            
            // Обновляем активную кнопку
            document.querySelectorAll('.time-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            event.target.classList.add('active');
            
            // Здесь можно загрузить данные за выбранный период
            console.log('Time range changed to:', range);
        }

        // Выход
        function logout() {
            localStorage.removeItem('token');
            localStorage.removeItem('user');
            window.location.href = '/';
        }
    </script>
</body>
</html>
